apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "daydesk.fullname" . }}
  labels:
    {{- include "daydesk.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      {{- include "daydesk.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "daydesk.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "daydesk.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 30
      {{- if .Values.initContainer.enabled }}
      initContainers:
        - name: prisma-migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ['node', 'node_modules/prisma/build/index.js', 'migrate', 'deploy']
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.postgresSecretName" . }}
                  key: database-url
            - name: DIRECT_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.postgresSecretName" . }}
                  key: {{ if or .Values.postgres.directUrl .Values.postgres.existingSecret }}direct-url{{ else }}database-url{{ end }}
                  {{- if .Values.postgres.existingSecret }}
                  optional: true
                  {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.postgresSecretName" . }}
                  key: database-url
            - name: DIRECT_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.postgresSecretName" . }}
                  key: {{ if or .Values.postgres.directUrl .Values.postgres.existingSecret }}direct-url{{ else }}database-url{{ end }}
                  {{- if .Values.postgres.existingSecret }}
                  optional: true
                  {{- end }}
            - name: APP_BASE_URL
              value: {{ .Values.appUrl | quote }}
            - name: NEXTAUTH_URL
              value: {{ .Values.appUrl | quote }}
            - name: AUTH0_DOMAIN
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.auth0SecretName" . }}
                  key: auth0-domain
            - name: AUTH0_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.auth0SecretName" . }}
                  key: auth0-secret
            - name: AUTH0_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.auth0SecretName" . }}
                  key: auth0-client-id
            - name: AUTH0_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "daydesk.auth0SecretName" . }}
                  key: auth0-client-secret
          {{- with .Values.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
