apiVersion: v1
kind: Service
metadata:
  name: {{ include "daydesk.fullname" . }}
  labels:
    {{- include "daydesk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "daydesk.selectorLabels" . | nindent 4 }}
