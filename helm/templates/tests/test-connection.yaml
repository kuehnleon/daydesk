apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "daydesk.fullname" . }}-test-connection"
  labels:
    {{- include "daydesk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "daydesk.fullname" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never
